VITE_TRACKER_API=/api

# Clerk — optional; leave blank for legacy email/password login
# Tracker uses its own sign-in routes (/login, /sign-up) and Kaana branding in the UI.
# Auth emails still use the Clerk application name unless you use a dedicated Clerk app.
VITE_CLERK_PUBLISHABLE_KEY=
