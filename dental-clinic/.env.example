# Local dev — booking uses same-origin /api/platform (Vite proxies to clinic-api :3010)
# VITE_CLINIC_TENANT=dentacare

# Production — Nitro server proxies /api/platform to clinic-api (Cloud Run env, not Vite)
# CLINIC_API_ORIGIN=https://clinic-api-xxxxx.asia-south1.run.app
